{{page-title
  (concat
    "Open Proces Huis"
    (if this.showEnvironment (concat " - " this.environmentName))
  )
}}

<AuModalContainer />
<AuToaster />
<AuApp class={{if this.overrideScrollOnHomePage "override-home-page" ""}}>
  <AppHeader />
  {{#if this.isBreadcrumbBarShown}}
    <NavigationBar />
  {{/if}}
  {{#if this.isProcessBarShown}}
    <ProcessBar />
  {{/if}}
  {{outlet}}
</AuApp>
{{page-title
  (concat
    "Open Proces Huis"
    (if this.showEnvironment (concat " - " this.environmentName))
  )
}}

<AuModalContainer />
<AuToaster />

<AuApp
  class="{{(if
      this.showEnvironment
      (concat 'au-c-app--environment au-c-environment--' this.environmentName)
    )}}"
>
  <AppHeader />
  {{outlet}}
</AuApp>
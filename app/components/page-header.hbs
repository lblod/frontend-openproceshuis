<header ...attributes>
  {{#if (has-block "action")}}
    <AuToolbar @border="bottom" @size="medium" as |Group|>
      <Group>
        <div>
          {{#each (breadcrumbs) as |breadcrumb|}}
            <p class="au-u-h-functional">
              {{#if (is-last breadcrumb (breadcrumbs))}}
                {{!-- {{breadcrumb.title}} --}}
              {{else}}
                <AuLink
                  @icon="arrow-left"
                  @skin="secondary"
                  @route={{breadcrumb.data.route}}
                  @model={{breadcrumb.data.model}}
                  @models={{breadcrumb.data.models}}
                >
                  {{breadcrumb.title}}
                </AuLink>
              {{/if}}
            </p>
          {{/each}}
          <AuHeading>
            {{yield to="title"}}
          </AuHeading>
          {{#if (has-block "subtitle")}}
          <AuHeading @level="3" @skin="6" class="au-u-margin-top-tiny">
            {{yield to="subtitle"}}
          </AuHeading>
          {{/if}}
        </div>
      </Group>
      <Group>
        {{yield to="action"}}
      </Group>
    </AuToolbar>
  {{else if (has-block "alert")}}
    <AuToolbar @size="large" as |Group|>
      <Group>
        <AuHeading class="au-u-margin-right">
          {{yield to="title"}}
        </AuHeading>
      </Group>
      <Group>
        {{yield to="alert"}}
      </Group>
    </AuToolbar>
  {{else}}
    <AuHeading>
      {{yield to="title"}}
    </AuHeading>
  {{/if}}
  
</header>
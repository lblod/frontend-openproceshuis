<AuButton
  ...attributes
  class="border-radius"
  @disabled={{not this.hasFiles}}
  @loading={{this.downloadFiles.isRunning}}
  {{on "click" (perform this.downloadFiles)}}
>
  {{this.buttonLabel}}
</AuButton>
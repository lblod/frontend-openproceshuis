import arrayComparison from 'frontend-openproceshuis/utils/array-comparison';
import { module, test } from 'qunit';

module('Unit | Utility | array-comparison', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = arrayComparison();
    assert.ok(result);
  });
});
